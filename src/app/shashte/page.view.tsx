import TableCheckers from "@/components/VCheckersTable/VCheckersTable";
import React from "react";

type Props = {};

const PageView = (props: Props) => {
  return (
    <div className={"container"}>
      <TableCheckers></TableCheckers>
    </div>
  );
};

export default PageView;
